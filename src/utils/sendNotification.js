export const sendNotification = async (token, message) => {
  // This is a placeholder for the actual FCM sending logic.
  console.log(`Sending notification to ${token}: ${message}`);
  return Promise.resolve();
};
